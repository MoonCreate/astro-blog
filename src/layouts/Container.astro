<div class="container">
    <div class="container-content">
        <slot/>
    </div>
</div>

<style>
    .container {
        .container-content {
            padding: var(--viewport-spacing);
            background-color: hsl(var(--color-crust));
            box-shadow: var(--shadow-elevation-low);
            border-radius: 6px;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }
    }

    html[data-theme="dark"] {
        .container {
            .container-content {
                border: 2px solid hsl(var(--color-overlay0));
                box-shadow: unset;
            }
        }
</style>